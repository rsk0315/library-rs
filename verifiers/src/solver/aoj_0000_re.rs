use crate::jury;
use crate::test_set::Solver;

pub struct Aoj0000Re {}

impl Solver for Aoj0000Re {
    type Jury = jury::Aoj0000;
    fn solve(_: ()) -> Vec<String> {
        panic!("nekochan");
    }
}
