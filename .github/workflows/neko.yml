name: neko

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        ref: doc

    - name: Nya
      run: |
        echo test
        git status
        echo nya > nya.txt
        ls
        git log
        git status
        git add nya.txt
        git commit -m Nya
        git push
