[package]
name = "cuckoo_hash_set"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
cuckoo_hash_map = { path = "../cuckoo_hash_map" }
